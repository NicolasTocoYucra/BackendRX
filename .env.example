PORT=5000
MONGO_URI=mongodcoName=Cluster0

JWT_SECRET=stkey
EMAIL_USER=dar1@gmail.com
EMAIL_PASS=fxiz dgev

# NODE_ENV=116522   ← valor anterior (no estándar, lo dejamos comentado)
NODE_ENV=development  # modo correcto para trabajar en tu máquina local

APP_NAME=Platafoantes
FRONTEND_URL=http://localhost:5173   # ajusta si tu frontend usa otro puerto
RESET_TOKEN_TTL_MIN=20               # minutos de validez del token de reset

# Rate limit de reenvío del 2FA
RESEND_WINDOW_SEC=600         # 10 minutos de ventana
RESEND_MAX_PER_WINDOW=3       # máx 3 reenvíos en esa ventana
RESEND_MIN_INTERVAL_SEC=60    # al menos 60s entre reenvíos
TWOFA_TTL_MIN=5               # validez del código (minutos)

AWS_ACCESS_KEY_ID=AKID3TAU2L3
AWS_SECRET_ACCESS_KEY=ibZi7SXzE/
AWS_REGION=us1
S3_BUCKET=hanslic
S3_PUBLIC_BASE=https:/aws.com
STORAGE_DRIVER=s3
